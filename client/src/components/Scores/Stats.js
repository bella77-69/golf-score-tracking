import React from 'react';

const Stats = () => {
    return (
        <div className="golf-course-statistics">
      <h2>Golf Course Statistics</h2>
      {/* Display average scores on each hole, average score for each course, and user-specific stats */}
    </div>
    );
};

export default Stats;